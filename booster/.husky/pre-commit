#!/usr/bin/env bash

# Git hook wrapper for ZX implementation

# Get the directory of this script
CWD="$(dirname "$0")"
HOOK_DIR="$(cd "$CWD" && pwd)"
cd "$HOOK_DIR"

# Use the generic runner script to execute the TypeScript hook
exec shared/runner.sh pnpm zx pre-commit.ts "$@"
